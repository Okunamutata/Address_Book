//input the user password and the encrypt key(For example: "key12345key12345" This is a 128 bit key).
    public void encrpytPassword (String password, String Key){
        Key aesKey = new SecretKeySpec(key.getBytes(), "AES");
        Cipher cipher = Cipher.getInstance("AES");
        cipher.init(Cipher.ENCRYPT_MODE, aesKey);
        byte[] encrpyted = ciper.doFinal(password.getBytes());
        password = new String(encrypted);
    }

    //input the encryped password and the decrypt key (same as the encrypted key).
    public void decrpytPassword (String password, String Key){
        Key aesKey = new SecretKeySpec(key.getBytes(), "AES");
        Cipher cipher = Cipher.getInstance("AES");
        cipher.init(Cipher.DECRYPT_MODE, aesKey);
        password = new String(ciper.doFinal(password.getBytes()));
    }